[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "hackathon-everest-isaaclab"
version = "0.1.0"
description = "External Isaac Lab extension for the Everest crampon policy"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["hackathon_everest_isaaclab*"]
